# Environment Configuration Schema
# Local deterministic baseline requires ZERO external API keys or secrets.
PORT=3000
NODE_ENV=development
NEXT_PUBLIC_APP_ENV=local
NEXT_PUBLIC_SITE_TIMEZONE=Asia/Calcutta
NEXT_PUBLIC_DEMO_MODE=true
